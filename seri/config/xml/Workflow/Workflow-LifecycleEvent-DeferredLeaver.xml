<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE sailpoint PUBLIC "sailpoint.dtd" "sailpoint.dtd">
<sailpoint>

  <EmailTemplate name="HR Termination Notification">
    <Description>
    </Description>
    <Body>
      <![CDATA[
      <html>
        <br>
        This is an automated notification from IdentityIQ.<br><br>
        An HR Termination event has been processed for $identityDisplayName.<br>
        Deprovisioning of access associated with this termination will commence on:  $termDisplayDate<br><br>
        Please click <a href="http://localhost:8080/identityiq/dashboard.jsf">here</a> for further details<br>
        <br><br>
        Sincerely,<br>
        IT Security Team
      </html>
      ]]>
    </Body>
    <Subject>HR Termination Notification for $identityDisplayName</Subject>
  </EmailTemplate>

<Workflow  explicitTransitions="true"  libraries="Identity"  name="Lifecycle Event - HR Terminate" type="IdentityLifecycle">
  <Variable input="true" name="trigger">
    <Description>The IdentityTrigger</Description>
  </Variable>
  <Variable input="true" name="event" transient="true">
    <Description>
      The IdentityChangeEvent.  It can be used to build
      the provisioning plan, but does not need to be
      persisted with the case, so marked as transient.
    </Description>
  </Variable>
  <Variable input="true" name="identityName">
    <Description>The name of the identity.</Description>
  </Variable>
  <Variable initializer="script:(identityDisplayName != void) ? identityDisplayName : resolveDisplayName(identityName)" input="true" name="identityDisplayName">
    <Description>
      The displayName of the identity being updated.
      Query for this using a projection query and fall back to the name.
    </Description>
  </Variable>
  <Variable name="plan">
    <Description>The provisioning plan, which is built by a service method. </Description>
  </Variable>
  <Variable editable="true" initializer="false" name="optimisticProvisioning">
    <Description>
      Set to true to enable optimistic provisioning.  This will cause
      changes to the entitlements compiled from role assignments to be 
      applied immediately to the identity cube rather than waiting
      for the next refresh/reaggregation after the provisioning system
      completes the request.
    </Description>
  </Variable>
  <Variable editable="true" initializer="true" name="foregroundProvisioning">
    <Description>
      Normally provisioning is done in a step that uses the "background"
      option to force the workflow to be suspend and be resumed in a
      background task thread.  This prevents the browser session from
      hanging since provision can sometimes take a long time.  For demos
      and testing it can be better to do this in the foreground so that
      provisioning will have been performed when control is returned to the
      user.  This prevents having to run the Perform Maintenance task to 
      see the resutls of the request.
    </Description>
  </Variable>
  <Variable initializer="spadmin" input="true" name="fallbackApprover">
    <Description>
      A String that specifies the name of the Identity that will 
      be assigned any approvals where the owner of the approver 
      can't be resolved. Example if the scheme is "owner" and the 
      application doesn't specify and owner.
    </Description>
  </Variable>
  <Variable initializer="LCM" input="true" name="source">
    <Description>
      String version of sailpoint.object.Source to indicate
      where the request originated.  Defaults to LCM.
    </Description>
  </Variable>
  <Variable initializer="true" name="trace">
    <Description>
      Used for debugging this workflow and when set to true trace
      will be sent to stdout.
    </Description>
  </Variable>
  <Variable name="project">
    <Description>
      ProvisioningProject which is just a compiled version of the ProvisioningPlan.
    </Description>
  </Variable>
  <Variable name="identityRequestId" output="true">
    <Description>
       The sequence id of the Identity request object which is stored in 
       the name field of the identity request.
    </Description>
  </Variable>
  <Variable name="cart" output="true">
    <Description>
       This variable includes all ApprovalItems that are part of 
       the request process and is updated during the AfterScript
       of the approval process by assimilating the decisions 
       and comments from the Approvals copy of the ApprovalItem.
    </Description>
  </Variable>
  <Variable name="TERMINATE_DATE_OFFSET" initializer="120">
    <Description>
      FOR DEMO:  The number of seconds to offset the HR termination event received.
    </Description>
  </Variable>
  <Description>Disable all accounts when an employee leaves the company.</Description>

  <RuleLibraries>
    <Reference class="sailpoint.object.Rule"  name="LCM Workflow Library"/>
  </RuleLibraries>

  <Step action="call:buildEventPlan" name="Build Provisioning Plan" resultVariable="plan">
    <Arg name="identityName" value="ref:identityName"/>
    <Arg name="op" value="Disable"/>
    <Arg name="event" value="ref:event"/>
    <Arg name="trigger" value="ref:trigger"/>
    <Description>Go through all of the user's links and build a plan.</Description>
    <Transition to="Calculate Termination Date"/>
  </Step>

  <Step name="Calculate Termination Date">
    <Script>
      <Source>
	<![CDATA[
         import sailpoint.tools.xml.XMLObjectFactory;
         import sailpoint.tools.Util;
 
         /*
         XMLObjectFactory f = XMLObjectFactory.getInstance();
         String xml = f.toXml(wfcontext.getWorkflow().getVariables());
         Util.writeFile("/tmp/wf-variables.xml", xml);
         */
       
         Date now = new Date();
	 long time = now.getTime();
                
	 // 
	 //
         long offset = Long.parseLong(TERMINATE_DATE_OFFSET);

	 time += (offset * 1000);
         workflow.put("termDate", time);
	
	]]>
      </Source>
    </Script>
    <Transition to="Schedule Termination"/>
  </Step>

  <Step name="Schedule Termination" action='call:scheduleWorkflowEvent'>
      <Arg name="workflow" value="Lifecycle Event - Leaver"/>
      <Arg name="owner" value="ref:identityName"/>
      <Arg name="requestName" value="Oracle HR Event:  Termination for $(identityDisplayName)"/>
      <Arg name="scheduleDate" value="ref:termDate"/>
      <Arg name="identityName" value="ref:identityName"/>
      <Arg name="identityDisplayName" value="ref:identityDisplayName"/>
      <Arg name="sponsor" value="ref:launcher"/>
      <!-- Bump this up to add more delay -->
      <Arg name="scheduleDelaySeconds" value="0"/>
      <!-- workaround for Bug #27980 -->
      <Arg name="catchExceptions" value="true"/>
      
      <Transition to="Notify"/>
  </Step>

  <Step name="Notify" action="sendEmail">
    <Arg name="template" value="HR Termination Notification"/>
    <Arg name="to" value="identityiq-admins@sailpointdemo.com"/>
    <Arg name="from" value="identityiq-noreply@sailpointdemo.com"/>
    <Arg name="termDisplayDate">
      <Script>
        <Source><![CDATA[
import java.util.Date;
import java.text.SimpleDateFormat;

// Expect: java.util.Date termDate
return new SimpleDateFormat("yyyy-MM-dd HH:mm:ss").format(termDate);
        ]]></Source>
      </Script>
    </Arg>
    <Arg name="identityDisplayName" value="ref:identityDisplayName"/>
    <Transition to="end"/>
  </Step>
  
  <Step icon="Stop" name="end"/>

</Workflow>

</sailpoint>
